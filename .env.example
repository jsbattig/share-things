# Docker Compose Environment Variables

# API and Socket URLs for the client
# Replace with your actual domain and port if using custom ports
API_URL=http://localhost
SOCKET_URL=http://localhost

# Backend Configuration
CORS_ORIGIN=http://localhost
SESSION_TIMEOUT=600000
SESSION_EXPIRY=86400000
LOG_LEVEL=info
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# Docker Compose Configuration
# Uncomment and modify these if you want to expose ports to the host
# FRONTEND_PORT=8080
# BACKEND_PORT=3001